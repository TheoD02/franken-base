home:
    path: /{path?}
    controller: Symfony\Bundle\FrameworkBundle\Controller\TemplateController
    defaults:
        template: base.html.twig
    requirements:
        # All but not /api
        path: '^(?!api).*$'

controllers:
    resource:
        path: ../src/
        namespace: App\
    type: attribute